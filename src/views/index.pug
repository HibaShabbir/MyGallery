doctype html
html
  head
    title Authentication Example
  body
    if user
      h1 Welcome, #{user}!
      p You are logged in.
      p Total session count: #{sessionCount}
      form(action='http://localhost:3001/logout' method='GET')
        button(type='submit') Logout
    else
      h1 Welcome Guest
      p You need to log in.
      a(href='/login') Login
